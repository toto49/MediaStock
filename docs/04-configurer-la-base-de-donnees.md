# 2. Configurer la base de données

Lance ton serveur MySQL puis crée la base de données avec la commande suivante :

```sql
CREATE
DATABASE mediastock;
```

Ensuite, importe le script SQL de structure situé dans le dossier `/sql`.
